# Validate decorators

Parses the output from the `scripts/pytest_mark_check.py`

Simply if the parsed string is an object, we treat that as a success, if it is a list, we treat it as an error

### Development

To get dependencies

```
npm ci
```